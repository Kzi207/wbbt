services:
  - type: web
    name: bot-web-manager
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 7070
      - key: NODE_ENV
        value: production
    disk:
      name: bot-data
      mountPath: /app/data
      sizeGB: 1
